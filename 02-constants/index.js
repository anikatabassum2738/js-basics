// variables whose value will change

let interestRate = 0.3;
interestRate = 1;
console.log(interestRate);

// variables whose value won't change

const interestRates = 0.3;
// interestRates = 1;
console.log(interestRates);

// if you don't need to reassign, use const. if you need to reassign, use let.