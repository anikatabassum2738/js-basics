// write afunction that takes two numbers and returns the maximum of the two.

let number = max(1, 3);
console.log(number);

function max(a, b) {
    if (a > b) return a;
    else return b;

    // return (a > b) ? a : b;
}

// (condition) ? a : b